# Wiki Log — 2026-07-10

> 이 날짜의 연산 기록 7건 (ingest 7). append-only, 시간 오름차순. 전체 날짜 색인: [[log-index]]

## [2026-07-10] ingest | DWH Monday 적재·저장소 설계 정정 (read/write 토큰·LangGraph 수집·md DocumentStore)
- raw: [[2026-07-10-dwh-monday-ingestion-refinements]]
- 배경: 사용자 후속 지시 — (1) Monday 발급 토큰이 read/write라 우리 코드(래퍼)로 읽기 전용 강제, (2) 스케줄러로 AI 자동 수집, (3) md 포맷 저장 + DB 교체 가능 설계(팀 LLM WIKI 대비). 후속 질의로 강제 수준(래퍼)·저장소 형태(DocumentStore 어댑터)·실행 방식(LangGraph) 확정.
- 생성: summary-dwh-monday-ingestion-refinements, decision-monday-readonly-client-wrapper, decision-monday-collector-langgraph-scheduled, decision-dwh-md-document-store (4페이지)
- 갱신: entity-monday-com(인증 모델 정정 — read/write 토큰 + 래퍼 강제, 관련 링크), decision-monday-ingest-hybrid(폴링 레인 실행체 = LangGraph 수집 에이전트·읽기 전용 래퍼 경유), concept-port-adapter(DocumentStore·Monday 래퍼 실체화 2건 추가), decision-index(DWH 그룹 3건), summary-index(DWH 그룹 1건), index.md(summary 21→22·decision 60→63·페이지 138→142·raw 25→26·날짜 2026-07-10)
- 핵심 결정: Monday 읽기전용 = 래퍼가 mutation 차단(코드적 강제, OAuth read-scope는 미전제) · 자동 수집 = 스케줄러 트리거 + LangGraph 에이전트(엔진 재사용, 폴링 레인 실행체) · 저장 = md 표준 포맷 + DocumentStore 포트/어댑터(PG 초기 → 향후 VectorStore로 교체, LLM WIKI 대비).
- 정정 성격: 2026-07-09 조사의 "personal 토큰 읽기전용 강제 불가/OAuth만 가능"은 참으로 유지하되, 실제 발급 토큰이 read/write라 우리 상황에선 래퍼 강제가 실질 방어임을 명시(raw 불변 — 새 raw 추가로 정정).

## [2026-07-10] ingest | 데이터 수집 커넥터 아키텍처 — Monday=첫 구현체, 수집→DWH→파이프라인 AI 쿼리
- raw: [[2026-07-10-ingestion-connector-architecture]]
- 배경: grill(설계 검증) 세션에서 실제 backend 코드 확인(connectors/는 SCM 전용·RunDocOutput 이미 md 저장·SourceScheduler/TagPoller 자동수집 패턴·DWH 테이블은 코드에 아직 없음) 후 사용자가 상위 아키텍처 확정 — Monday를 데이터 수집 커넥터 첫 구현체로, 다른 소스도 확장 가능하게, 스케줄러로 DWH 적재, 파이프라인이 DWH를 AI로 쿼리.
- 생성: concept-ingestion-connector, decision-ingestion-connector-architecture, decision-document-identity-run-separation, summary-ingestion-connector-architecture (4페이지)
- 갱신: entity-monday-com(수집 커넥터 첫 구현체 역할 + 관련 링크), concept-port-adapter(IngestionConnector 실체화 = SCM과 형제 포트), concept-index(1건), decision-index(DWH 그룹 2건), summary-index(1건), index.md(summary 22→23·concept 8→9·decision 63→65·페이지 142→146·raw 26→27)
- 핵심 결정: (1) Monday=IngestionConnector 첫 구현체, ScmConnector와 형제 포트(상속 아님 — 코드 읽기/제출 vs 데이터 추출/적재 책임 상이). (2) 4층 흐름 = 수집 커넥터 → DWH(bronze/silver/gold) → 파이프라인이 DWH를 AI 쿼리 → 산출 md는 DocumentStore. (3) grill 합의 2건 파일링 = DocumentStore가 RunDocOutput 흡수·일반화 + 문서 정체성 run 분리(run_id nullable, Document=오래 사는 엔티티).
- 성격: 어제까지의 개별 Monday/DWH 결정을 하나의 상위 축으로 묶음. 코드 근거 확인 후 반영 — 기존 자산(포트/어댑터·스케줄러·RunDocOutput·run 인프라) 재사용 원칙 명문화.

## [2026-07-10] ingest | 커넥터 설정 = 시스템 설정 페이지
- raw: [[2026-07-10-connector-settings-in-system-settings]]
- 배경: 사용자 지시 "커넥터 설정은 시스템 설정 페이지로 구성". 코드 확인 — SystemSetting/SettingsService가 이미 `llm.*` 네임스페이스를 DB우선→.env폴백·redact·updated_by 감사로 운영 중.
- 생성: decision-connector-settings-system-settings (1페이지)
- 갱신: decision-ingestion-connector-architecture(설정 위치 연결), concept-ingestion-connector(설정 위치 링크), decision-index(1건), index.md(decision 65→66·페이지 146→147·raw 27→28)
- 핵심 결정: 커넥터 설정을 `SystemSetting`의 `connector.<kind>.*` 새 네임스페이스로 저장(enabled·api_token·schedule_cron·board_ids 등), 기존 llm.* 패턴 재사용. SCM 소스 등록(Source/ScmInstance, 레포×브랜치)과 구분 — 커넥터는 소스(SaaS) 단위 시스템 설정.
- 열린 항목: SystemSetting은 v1 평문 저장인데 Monday 토큰은 read/write 민감 토큰 → connector.*.api_token은 SecretBox 암호화 + list_recent redact 대상 포함 필요(구현 시 확정).

## [2026-07-10] ingest | bronze 저장 = 소스 구분 없는 단일 JSONB 테이블 (raw_records)
- raw: [[2026-07-10-bronze-single-jsonb-table]]
- 배경: grill(개발 시 애매한 지점) 세션 — `IngestionConnector.to_bronze`가 선언돼 있으나 bronze row를 넣을 테이블이 backend 코드에 없음(models.py 전부 run 중심, bronze/silver/gold 부재). "어디에 INSERT하나"에서 착수가 막힘. 사용자 결정 "서랍 하나에 넣자".
- 생성: decision-bronze-single-jsonb-table (1페이지)
- 갱신: decision-index(DWH 그룹 1건 추가), index.md(decision 66→67·페이지 147→148·raw 28→29)
- 핵심 결정: bronze 착륙 = 소스별 전용 테이블 대신 단일 범용 테이블 `raw_records(connector_kind, source_id, external_id, payload_jsonb, content_sha256, watermark, ingested_at)`. 원본 JSON을 JSONB에 통째 보존(변환 금지), 소스 추가 시 커넥터만 추가·스키마 변경 0. RunDocOutput의 content_sha256(중복감지)·deferred(리스트조회 시 원문 미로드) 관례 재사용.
- 정합: 저장 위치는 이미 [[decision-dwh-storage-postgres-single]]가 `bronze` 스키마로 확정 → raw_records는 그 스키마에 둠(이 결정은 스키마 내 테이블 형태만 확정). silver/gold 이력은 [[decision-dwh-scd-strategy]] SCD2와 층이 다름.
- 열린 항목: 같은 external_id 재수집 시 bronze에 append(스냅샷 이력 보존) vs upsert(현재만) — SCD2 구현·문서 식별 정책과 맞물림.

## [2026-07-10] ingest | bronze 재수집 정책 = append(sha256 동일 시 skip) 확정 — 직전 결정의 열린 항목 승격
- raw: [[2026-07-10-bronze-single-jsonb-table]] (동일 raw — 열린 항목의 답)
- 배경: grill 질문 "같은 Monday item을 매일 재수집하면 bronze에 어떻게 쌓나". 사용자 결정 "B로 우선 갑니다" = append.
- 생성: 없음 (새 페이지 억지 생성 금지 — 직전 결정 [[decision-bronze-single-jsonb-table]]의 열린 항목을 확정으로 승격)
- 갱신: decision-bronze-single-jsonb-table(결정 본문에 재수집 정책=append 명시 + 근거 "append가 SCD2와 맞물린다" 추가 + 열린 항목을 sha256 정규화·파티셔닝 2건으로 교체)
- 핵심 결정: 같은 external_id를 매일 다시 받아도 **덮어쓰지 않고 새 행 append**, 단 직전분과 content_sha256 같으면 skip. bronze=매 수집 스냅샷의 append-only 로그 → silver의 SCD2(dbt snapshot) 입력이 됨. upsert면 SCD2가 변경 근거를 잃어 취약하므로 기각.
- 열린 항목(승격 후 남은 것): sha256 정규화 기준(오탐 방지), append 로그 보존기간/시간 파티셔닝.

## [2026-07-10] ingest | DWH 저장을 Karpathy LLM Wiki 구조로 재조직 (축적·기록·정리 3동작)
- raw: [[2026-07-10-dwh-storage-as-karpathy-llm-wiki]]
- 배경: 사용자 방향 전환 지시 — "안드레이 카파시의 LLM WIKI 구조를 따라야 한다. 원본은 계속 쌓고, 변경점을 기록하고, 핵심 데이터는 정리하라." 저장 설계를 다시 하라. 조사 에이전트로 Karpathy 원형을 1차 출처(Gist llm-wiki.md + 원 트윗)로 교차 확인 — 원형 3계층은 raw/wiki/schema이고 log는 wiki 하위 파일(질문의 raw/log/distilled 3계층은 부정확). ingest/query/lint·raw불변·compounding 확인.
- 생성: decision-dwh-as-karpathy-llm-wiki, concept-karpathy-llm-wiki-storage (2페이지)
- 갱신: decision-monday-ingest-hybrid(정정 노트 — bronze 저장방식 upsert+전용테이블 → append+raw_records, 2레인은 유지), decision-bronze-single-jsonb-table(상위 철학 링크), concept-medallion-dwh-on-postgres(같은 discipline의 LLM Wiki 언어 링크), concept-index(1건), decision-index(DWH 그룹 상위 축 1건), index.md(concept 9→10·decision 67→68·페이지 149→150·raw 29→30)
- 핵심 결정: 저장의 1차 조직 언어를 BI(Kimball)에서 Karpathy 3동작으로 올림 — ① 원본 축적=raw_records append(sha256 skip), ② 변경 기록=append 스냅샷 히스토리 + _meta.ingest_log, ③ 핵심 정리=silver/gold(SCD2 이력). medallion bronze/silver/gold는 그 아래 물리 계층으로 유지(supersede 아님, 같은 layering discipline의 다른 언어).
- 봉합: 어제 decision-monday-ingest-hybrid의 "upsert+monday_webhook_events_raw 전용테이블"과 오늘 "append+단일 raw_records" 충돌 → 저장 방식만 후자로 정정(정정 노트), webhook event_id idempotency는 "동일 event_id skip"으로 흡수, 2레인(webhook+야간전수) 트리거 축은 유지.
- 열린 항목: _meta.ingest_log 스키마·기존 audit_logs와 역할 분담, ③ 계층을 silver/gold 유지 vs summaries/entities/concepts 재명명, 소스 확장 시 단일테이블+로그 파티셔닝.

## [2026-07-10] ingest | Monday 적재 = 폴링 단일 레인으로 단순화 (webhook 삭제) + 백엔드 코드 정합 검증
- raw: [[2026-07-10-monday-ingest-polling-only]]
- 배경: query(Monday 커넥터 연결 정리) 답변에 대해 사용자 두 지적 — (1) "적재 전략(webhook 하이브리드) 필요 없다, 파이프라인 있잖아", (2) "착륙지점은 카파시 저장 아키텍처 반영해야". AskUserQuestion으로 확정: (1) webhook 삭제·폴링 단일 레인, 지연=일 배치 수용, (2) 카파시는 이미 decision-dwh-as-karpathy-llm-wiki로 반영됨 → 답변만 정정.
- 생성: decision-monday-ingest-polling-only (1페이지)
- supersede: decision-monday-ingest-hybrid → status superseded (webhook 레인 삭제, 폴링 단일 레인이 계승; 상호 링크)
- answered: question-dwh-latency-target → status answered (일 배치 확정 → webhook 불필요; blocking 태그 없음)
- 갱신: concept-readonly-saas-cdc(하이브리드 정석 → 폴링 단일 기본·webhook은 근실시간 옵션으로 정정), decision-monday-collector-langgraph-scheduled(폴링 레인 실행체 링크), decision-monday-readonly-client-wrapper·entity-monday-com·decision-dwh-as-karpathy-llm-wiki·decision-dwh-transform-dbt·question-monday-plan-tier·summary-dwh-monday-ingestion-refinements(hybrid→polling-only 링크 정정), overview(적재 서술), decision-index(hybrid superseded 표기 + polling-only 신규), question-index(2건), index.md(decision 68→69·superseded 10→11건·question answered 23→24건·페이지 150→151·raw 30→31)
- 핵심 결정: 정확성은 폴링이 전담, webhook은 latency만 줄이는 옵션 → 지연 목표=일 배치면 폴링 단일 레인. 근실시간 요구 시 webhook 옵션 복귀(패턴은 concept-readonly-saas-cdc에 보존).
- 백엔드 코드 정합 검증(사용자 질의 "백엔드 개발 진행사항 잘 반영했나?"): 위키 근거 "기존 자산 재사용"이 실제 코드와 일치 확인 — ScmConnector 포트(connectors/base.py, read+write+auth 양방향)·SourceScheduler(APScheduler BackgroundScheduler+CronTrigger, 태그 폴링 잡 이미 등록)·SystemSetting/Source/SourceSchedule(schedule_cron)·RunDocOutput.content_sha256(String64)+content_text deferred 모두 존재. **단, DWH·Monday·IngestionConnector·DocumentStore·raw_records·connector.monday.* 는 코드에 아직 없음 = 전부 설계(design)이며 구현 착수 전.** 위키는 "구현 계획"을 정확히 기록 중, 코드 회귀 아님.
