# Wiki Log — 2026-07-09

> 이 날짜의 연산 기록 3건 (schema 2 · ingest 1). append-only, 시간 오름차순. 전체 날짜 색인: [[log-index]]

## [2026-07-09] schema | 검증기·템플릿 이식 (스키마 도구화)
- 배경: 자매 프로젝트(integration_nvr_dvr)의 스키마 개선에서 만든 자동 검증기·타입별 템플릿을 이 위키에 이식. 기존 schema.md 규약·디렉터리 구조(raw/·wiki/·schema.md, 영문, lazy-loading 2계층 인덱스)는 **변경 없이 존중** — 규약을 코드/템플릿으로 실행 가능하게만 보강.
- 신규: `validate_frontmatter.py` — schema.md의 정적 lint 항목을 코드화(필수 4필드·type 6종·status enum(type별)·type↔폴더·파일명 접두사·answered question의 blocking 잔존). **121개 지식 페이지 위반 0 (PASS)**.
- 신규: `templates/` 6종 (overview·summary·entity·concept·decision·question) — 각 유형의 frontmatter·콘텐츠 초점·라이프사이클 주석 포함.
- 갱신: schema.md — 3계층 표 아래 "스키마 도구" 문단(검증기·템플릿 경로), frontmatter 절에 type별 status 허용값 명시+템플릿 안내, lint 절에 검증기 우선 실행 명령 추가.
- 실측 현황(역산): type 6종 = decision 54·question 34·summary 21·entity 6·concept 5·overview 1. status = decision(active 44·superseded 10), question(answered 23·open 11), 그 외 active. 전부 schema.md 정의와 정합.
- 코드/위키 페이지 콘텐츠 변경 없음(도구·규약 문서만).

## [2026-07-09] schema | 디렉터리 재편 — log를 wiki/log/ 날짜별로 분리 + schema/ 폴더 통합 (Karpathy 정합)
- 계기: 사용자 지적 — Karpathy LLM Wiki 원안 대비 (1) `log.md`가 위키 밖(`docs/log.md`)에 있어 "위키가 곧 상태" 원칙과 어긋남, (2) 스키마·템플릿·검증기가 `docs/`에 분산. 원문·대표 구현(SKILL.md) 재확인: 원안은 log·index를 `wiki/` 안에 두고 템플릿을 schema 인접에 둔다.
- **log 날짜별 분리**: `docs/log.md`(67항목) → `docs/wiki/log/<YYYY-MM-DD>.md` 5개(07-05:34·07-06:25·07-07:6·07-08:1·07-09:2) + `log-index.md`(날짜별 진입 색인, 최신 위). 스크립트로 블록 단위 분할 — 67블록 바이트 단위 완전 일치 검증(내용·요약·삭제 무변경, append-only 감사 추적 보존). index 2계층화와 같은 lazy-loading 원리를 log에 적용.
- **schema 폴더 통합**: `git mv`로 `schema.md`·`templates/`(6종)·`validate_frontmatter.py` → `docs/schema/`. 3계층이 `docs/` 안에서 raw/·wiki/·schema/로 나란히 정렬. 검증기 `WIKI_ROOT` 경로 재계산 + `wiki/log/`(frontmatter 없는 운영 파일)를 검사 대상에서 제외.
- **진입점 신설**: `docs/home.md` — vault 홈. overview(서사)·index(카탈로그)·log-index(감사)·schema(헌법)로 안내하는 네비게이션 허브.
- **규약 개정(schema.md)**: 3계층 표(경로·log 위치), 보조 파일 문단(log 2계층), 스키마 도구 경로, ingest step5·query step3·lint 절의 "log 기록"을 날짜 파일+log-index로, log 형식 절(날짜별 파일 구조), lint 검사에 "log 색인 불일치" 항목 추가.
- **참조 일괄 갱신**: CLAUDE.md(3계층 설명·경로·명령·변경 이력), 스킬 4종(ingest·query·lint·wiki-ops — schema.md 경로·grep 명령·log 기록·완료 게이트), 에이전트 3종(ingestor·librarian·linter), wiki/index.md(log-index 진입점·날짜).
- 검증: `python docs/schema/validate_frontmatter.py` PASS(121페이지 위반 0, log 폴더 오탐 0) · log 항목 67 대조 일치 · wiki+home wikilink 1480개 중 신규 링크 전부 해소(기존 [[lint-report-2026-07-06]] 3건은 과거 이력의 삭제된 대상 — 소급 미수정 기판정 유지).
- 원칙: raw/ 무수정 · log 내용 완전 보존(분리만) · git mv로 이력 보존 · schema.md가 SSOT.

## [2026-07-09] ingest | DataWarehouse 설계 계획 (Monday.com + wiki_pipeline → PostgreSQL)
- raw: [[2026-07-09-dwh-design-plan]]
- 배경: 사용자 요청 "Monday.com read-only API Key로 DataWarehouse 구축, 우리 파이프라인 산출물도 통합, 어떻게 설계하는지 상세 계획". 두 갈래 librarian 병렬 조사(Monday API 데이터 모델 · DWH 설계 패턴 결정 공간) + control plane models.py 직접 확인에 근거.
- 생성: summary-dwh-design-plan, entity-monday-com, entity-data-warehouse, concept-medallion-dwh-on-postgres, concept-monday-column-value-modeling, concept-readonly-saas-cdc, decision-dwh-shape-kimball-medallion, decision-dwh-storage-postgres-single, decision-monday-ingest-hybrid, decision-dwh-column-value-hybrid, decision-dwh-scd-strategy, decision-dwh-transform-dbt, question-monday-plan-tier, question-monday-user-mapping, question-monday-item-source-key, question-dwh-data-volume, question-dwh-latency-target, question-dwh-multi-tenancy (18페이지)
- 갱신: overview.md(DWH 섹션 신설 — 세 번째 시스템 축), index.md(페이지 120→138·raw 24→25), decision-index(데이터 웨어하우스 그룹 6건), concept-index(3건), entity-index(2건), question-index(데이터 웨어하우스 그룹 6건), summary-index(데이터 웨어하우스 그룹 1건)
- 핵심 결정: Kimball+Medallion on PostgreSQL 단일 클러스터 · Monday webhook+야간 폴맨스 하이브리드 · typed long table+JSONB 폴백 · SCD entity별 혼합 · dbt+cron-first.
- 열린 질문 6건은 진행 블로커 아닌 사용자 결정 항목(plan tier·SSO·item↔repo 키·볼륨·지연·다부서).
